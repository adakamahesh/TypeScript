"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var user; // Declaring a tuple 'user' with elements of type string, number, and boolean
user = ['mahesh', 501, true]; // Assigning values to the 'user' tuple
console.log(user);
var rgb = [23, 32, 52]; // Declaring a tuple 'rgb' with elements of type number, number, and number
var newuser = [122, 'mahesh']; // Declaring a 'newuser' of type 'user' and assigning it a tuple value
