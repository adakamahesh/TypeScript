let user:[string,number,boolean]// Declaring a tuple 'user' with elements of type string, number, and boolean
user=['mahesh',501,true]// Assigning values to the 'user' tuple
console.log(user);

let rgb:[number,number,number]=[23,32,52]// Declaring a tuple 'rgb' with elements of type number, number, and number

type user=[number,string]// Defining a type 'user' as a tuple with elements of type number and string
const newuser:user=[122,'mahesh']// Declaring a 'newuser' of type 'user' and assigning it a tuple value


export{}